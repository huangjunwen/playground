/** Public surface of @playground/vendor-assets. */

export type { AssetEntry } from './registry';
export {
  getAssetInfo,
  listAssets,
  listFamilies,
  resolveAssetPath,
  resolveAssetUrl,
} from './registry';
